name: telegram-member-checker
type: web
regions:
  - fra  # Frankfurt region

env:
  - key: API_ID
    value: "your_api_id"
  - key: API_HASH
    value: "your_api_hash"
  - key: BOT_TOKEN
    value: "your_bot_token"

build:
  dockerfile: Dockerfile

run:
  command: python main.py
